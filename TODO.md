# TODO: Fix Vercel Build Failure

## Steps to complete:
1. [x] Edit `src/index.css`: Replace `justify-content: start;` with `justify-content: flex-start;` in `.category-btn` class.
2. [x] Edit `package.json`: Update `browserslist.production` to `">0.2%, not dead, not op_mini all, last 2 Chrome versions"`.
3. [x] Run `npm run build` to verify locally (ongoing).
4. [ ] Confirm build success, update TODO.
5. [ ] Commit/push changes to trigger Vercel redeploy.
6. [Done]
